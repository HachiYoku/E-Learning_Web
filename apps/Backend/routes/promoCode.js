const router = require('express').Router(); const validateToken = require('../middleware/authMiddleware'); const requireAdmin = require('../middleware/adminMiddleware'); const c = require('../controllers/promoCodeController');
router.post('/validate', validateToken, c.validatePromo); router.get('/admin', validateToken, requireAdmin, c.listPromos); router.post('/admin', validateToken, requireAdmin, c.createPromo); router.put('/admin/:id', validateToken, requireAdmin, c.updatePromo); router.delete('/admin/:id', validateToken, requireAdmin, c.deletePromo); module.exports = router;
