VITE_API_URL=https://api.example.com
# Optional when Socket.IO is hosted at a different public origin than the API.
# Otherwise the frontend connects to VITE_API_URL automatically.
VITE_SOCKET_URL=https://api.example.com
